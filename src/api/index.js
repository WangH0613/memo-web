import device from '@/api/moudles/device';
import model from '@/api/moudles/model';
import combo from '@/api/moudles/combo';
import technical from '@/api/moudles/technical';
import pub from '@/api/moudles/pub';
import total from '@/api/moudles/total';
import manufacturer from '@/api/moudles/manufacturer';
import select from '@/api/moudles/select';
import user from '@/api/moudles/user'
import itsm from '@/api/moudles/itsm'
import scheme from '@/api/moudles/scheme'
import hardware from '@/api/moudles/hardware'
import server from '@/api/moudles/server'

export default {
  device,
  model,
  combo,
  technical,
  pub,
  total,
  manufacturer,
  select,
  user,
  itsm,
  scheme,
  hardware,
  server
}
